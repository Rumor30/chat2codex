# Changelog

## 0.2.0 — 2026-09-11

### 新增

- 控制台四步配置向导：独立登录、私有 Tunnel 配置、受保护的 session 设置助手和逐模型 MCP 探针。
- 可取消、跨控制台刷新保留的配置操作进度；进程中断不自动重放创建。
- 账号重命名、1–5 任务槽、停用/恢复、归档/恢复、单线程释放、脱敏诊断。
- 一次性控制台自动登录邀请、启动脚本和独立的运行环境检查。
- 完整公开模型目录模板校验和临时 Codex catalog 注入，兼容当前 `model_messages` 格式。
- 实际 Chromium UI/合成 Connector DOM、实际 Electron 空白账号启动和真实 Codex CLI 的独立集成测试。

### 修复

- 生成上游启动器实际需要的 `.launcher-runtime/browser-helper.cjs`。
- High 验证不再放行未验证的 Pro；登录身份/配置变化后旧验证失效。
- 配置维护期间阻止新任务；同时提交操作/取消与创建之间的竞争受到约束。
- 已释放线程转存到持久化哈希重放保护，不继续消耗活跃路由容量。
- 状态写入失败不再泄露内存锁或导致 HTTP 控制台直接崩溃。
- 严格区分重复、缺失和不相干的工具回执；拒绝矛盾和重复终结事件。
- Windows 执行原生 Codex 入口，不对任务参数进行 shell 拼接。

### 尚未完成的生产验收

- 真实登录后的当前 ChatGPT 设置页面适配、完整 MCP/Tunnel/工具链。
- 两个真实账号并发、图片回执、真实文件补丁、长任务压缩、断线恢复场景。
- 无需安装依赖的签名桌面安装包和 Codex App 完整接入。

## 0.1.0

初始账号池、HTTP/SSE Responses 网关、线程粘性、无配置写入的 Codex CLI wrapper 和明确的开发预览验证记录。
